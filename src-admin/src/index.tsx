// Only a placeholder: the production bundle is built by Module Federation from `Components.tsx`,
// and admin renders the component inside its own JsonConfig page - there is no standalone app.
export {};
