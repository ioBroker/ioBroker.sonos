// The module admin loads through Module Federation. The component is addressed in
// `admin/jsonConfig.json` as `ConfigCustomSonosSet/Components/SonosControlComponent`.
import SonosControlComponent from './SonosControlComponent';

export default { SonosControlComponent };
