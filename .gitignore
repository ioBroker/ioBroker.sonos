node_modules
.idea
admin/i18n/flat.txt
admin/i18n/*/flat.txt
iob_npm.done
.DS_Store
#ignore .commitinfo created by ioBroker release script
.commitinfo

# compiled typescript
build/
*.tsbuildinfo

# vis-2 widget sources and build output (built by "npm run build:widgets", shipped via prepublishOnly)
src-widgets/node_modules/
src-widgets/build/
src-widgets/.__mf__temp/
widgets/sonos/assets/
widgets/sonos/customWidgets.js
widgets/sonos/mf-stats.json
widgets/sonos/mf-manifest.json
widgets/sonos/img/

# control page for the web adapter: built by "npm run build:web" and shipped via prepublishOnly,
# so www/ is generated and not committed - `iobroker upload sonos` puts it into the file storage
src-web/node_modules/
src-web/build/
www/

# JsonConfig custom component and ioBroker.devices widgets: the sources are built with vite and
# the result is committed (admin/custom/, admin/dm-widgets/), because the module federation build
# takes minutes - see tasks.js
src-admin/node_modules/
src-admin/build/
src-admin/.__mf__temp/
src-devices/node_modules/
src-devices/build/
src-devices/.__mf__temp/
